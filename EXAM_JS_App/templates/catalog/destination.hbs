<a href="/details/{{_id}}" class="added-destination">
    <img src="{{imgUrl}}"
        alt="IMG" class="picture-added-destination">
    <h3>{{destination}}</h3>
    <span>to {{city}}</span><span> {{departureDate}}</span>
</a>